# QA-Subagent Memory Index

## Project

- [bds-date-picker-default-withtime-half-applied-bug.md](bds-date-picker-default-withtime-half-applied-bug.md) — EOA-17138 Task 15c: `calendarType='default'`+`withTime` blank-field/spurious-warning bug found live, then confirmed fixed on re-verification via `effectiveWithTime` getter; min/max regression also confirmed fixed.
- [bds-date-picker-playground-task3-scenarios-silently-default-mode.md](bds-date-picker-playground-task3-scenarios-silently-default-mode.md) — Task 15b's `calendarType` default flip (basic→default) silently broke 8 pre-existing Task 3 `index.html` scenarios that don't pin `calendar-type` explicitly; `dp-preload` now shows a blank field, contradicting its own documented steps. Reported, not fixed.
- [bds-date-picker-task15g-react-vue-parity.md](bds-date-picker-task15g-react-vue-parity.md) — Task 15g: `calendarType="default"` confirmed identical across web components/React/Vue (all 4 scenarios pass). Found the same "silently default mode" regression class recurring — this time in `examples/react-testapp`/`examples/vue-testapp`'s pre-existing Task 7/Task 15 scenarios, never retrofitted like `index.html` was. Logged as Task 15h, not fixed inline.
- [bds-date-picker-task18-range-dual-calendar.md](bds-date-picker-task18-range-dual-calendar.md) — Task 18 range/dual-calendar manual test: all scenarios pass. Also documents a Stencil async-render timing gotcha when click+read are split across separate `playwright-cli eval` calls too fast.
- [bds-date-picker-task18a-range-face-nested-textfield-invalid.md](bds-date-picker-task18a-range-face-nested-textfield-invalid.md) — Task 18a range FACE validity manual test: real bug found — slotted `bds-text-field` is independently form-associated and stays invalid (never synced) after a range Apply, blocking native form submission even though `bds-date-picker`'s own internals are valid. Reported, not fixed.
- [safari-do-javascript-automation-gotchas.md](safari-do-javascript-automation-gotchas.md) — EOA-17138 Task 20 Safari re-verification: real Safari.app automation via AppleScript `do JavaScript` (when Playwright-WebKit/System-Events/safaridriver are all unusable) needs `detail: 1` on synthetic MouseEvents (`bds-button`'s `event.detail === 0` guard) AND the target tab must be frontmost or Stencil's `rAF`-gated re-render silently never paints (state updates fine, DOM doesn't) — easily misdiagnosed as a real regression.

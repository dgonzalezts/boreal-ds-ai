# QA-Subagent Memory Index

## Project

- [bds-date-picker-default-withtime-half-applied-bug.md](bds-date-picker-default-withtime-half-applied-bug.md) — EOA-17138 Task 15c: `calendarType='default'`+`withTime` blank-field/spurious-warning bug found live, then confirmed fixed on re-verification via `effectiveWithTime` getter; min/max regression also confirmed fixed.
- [bds-date-picker-task48b-opaque-overlay-hides-dimmed-grid.md](bds-date-picker-task48b-opaque-overlay-hides-dimmed-grid.md) — Task 48b: overlay's opaque, near-identical-size background fully hides the dimmed day grid despite correct `aria-hidden`/`inert`/`--dimmed` DOM state — geometry+opacity check (or screenshot) needed, not just DOM attributes.
- [bds-date-picker-playground-task3-scenarios-silently-default-mode.md](bds-date-picker-playground-task3-scenarios-silently-default-mode.md) — Task 15b's `calendarType` default flip (basic→default) silently broke 8 pre-existing Task 3 `index.html` scenarios that don't pin `calendar-type` explicitly; `dp-preload` now shows a blank field, contradicting its own documented steps. Reported, not fixed.
- [bds-date-picker-task15g-react-vue-parity.md](bds-date-picker-task15g-react-vue-parity.md) — Task 15g: `calendarType="default"` confirmed identical across web components/React/Vue (all 4 scenarios pass). Found the same "silently default mode" regression class recurring — this time in `examples/react-testapp`/`examples/vue-testapp`'s pre-existing Task 7/Task 15 scenarios, never retrofitted like `index.html` was. Logged as Task 15h, not fixed inline.
- [bds-date-picker-task18-range-dual-calendar.md](bds-date-picker-task18-range-dual-calendar.md) — Task 18 range/dual-calendar manual test: all scenarios pass. Also documents a Stencil async-render timing gotcha when click+read are split across separate `playwright-cli eval` calls too fast.
- [bds-date-picker-task18a-range-face-nested-textfield-invalid.md](bds-date-picker-task18a-range-face-nested-textfield-invalid.md) — Task 18a range FACE validity manual test: real bug found — slotted `bds-text-field` is independently form-associated and stays invalid (never synced) after a range Apply, blocking native form submission even though `bds-date-picker`'s own internals are valid. Reported, not fixed.
- [bds-date-picker-task34b-expanded-no-range-warnings.md](bds-date-picker-task34b-expanded-no-range-warnings.md) — Task 34b: expanded+range=false dual warnings and duplicate-highlight fix confirmed passing (all 3 checklist items + regression check). Also documents a style-guidelines dist-missing dev-server build failure distinct from the known stale-index.html issue.
- [safari-do-javascript-automation-gotchas.md](safari-do-javascript-automation-gotchas.md) — EOA-17138 Task 20 Safari re-verification: real Safari.app automation via AppleScript `do JavaScript` (when Playwright-WebKit/System-Events/safaridriver are all unusable) needs `detail: 1` on synthetic MouseEvents (`bds-button`'s `event.detail === 0` guard) AND the target tab must be frontmost or Stencil's `rAF`-gated re-render silently never paints (state updates fine, DOM doesn't) — easily misdiagnosed as a real regression.
- [bds-date-picker-presets-s1-reopen-custom-mismatch.md](bds-date-picker-presets-s1-reopen-custom-mismatch.md) — EOA-17662 exploratory reopen check on `dp-presets-s1`: after applying "Today" preset, reopening the popover shows correct header/calendar/trigger-field values but the presets sidebar always falls back to "Custom" selected, never re-deriving "Today" — observational finding, intent not yet confirmed.
- [bds-date-picker-task34a-preset-rematch-fix-verified.md](bds-date-picker-task34a-preset-rematch-fix-verified.md) — Task 34a: `matchPreset` reverse-lookup on popover reopen confirmed correct for exact match, false-positive avoidance, and with-time coverage-shift, verified via DOM `className` not just ARIA state.
- [bds-date-picker-task34a2-shift-hydration-fix-verified.md](bds-date-picker-task34a2-shift-hydration-fix-verified.md) — Task 34a-2: `correctHydratedRangeEnd` fix confirmed live across expanded-preset, basic-manual, expanded-manual scenarios — no more duplicate highlight, header double-shift, or Apply→reopen→Apply drift.
- [bds-popover-shadow-is-filter-not-boxshadow.md](bds-popover-shadow-is-filter-not-boxshadow.md) — `bds-popover`'s drop shadow is CSS `filter: drop-shadow()`, not `box-shadow` — check `filter` in computed-style assertions, not `boxShadow`. Also documents the confirmed-correct baseline computed-style values (border-radius 2px, padding 12px 24px) for popover container audits.
- [dev-pack-react-vue-serial-not-parallel.md](dev-pack-react-vue-serial-not-parallel.md) — `dev:pack:react`/`dev:pack:vue` each end in their own persistent vite dev server; never chain with `&&`, never launch both at once.
- [qa-subagent-synthetic-click-vs-real-click.md](qa-subagent-synthetic-click-vs-real-click.md) — `element.click()`/synthetic `dispatchEvent` on a `bds-button`'s internal `<button>` silently no-ops; use a real Playwright locator click for any interaction test.
- [bds-date-picker-task23-range-time-selector.md](bds-date-picker-task23-range-time-selector.md) — Task 23 manual QA: two blocking bugs found — mouse click on any time-select option closes the whole popover (keyboard works fine); `range && withTime` Apply commits nothing in both `basic`/`expanded`. Auto-format header confirmed passing.
- [bds-date-picker-empty-format-string-crash.md](bds-date-picker-empty-format-string-crash.md) — `format=""` (explicit empty string, distinct from omitting the prop) crashes date-fns formatting universally across web components/React/Vue; pre-existing bug found during EOA-17662 Task 27, needs a frontend-subagent fix. Moved here from the incorrectly-placed `packages/boreal-web-components/.claude/agent-memory/qa-subagent/` on 2026-09-09.
- [concurrent-dev-pack-builds-race-condition.md](concurrent-dev-pack-builds-race-condition.md) — running `dev:pack:react`/`dev:pack:vue` concurrently corrupts the shared `boreal-web-components` dist output (spurious `TS2306` failures); companion detail to [[dev-pack-react-vue-serial-not-parallel]], also covers a third-party live Stencil watcher as a second race source. Moved here from the incorrectly-placed `packages/boreal-web-components/.claude/agent-memory/qa-subagent/` on 2026-09-09.
- [bds-date-picker-task31-presets-focus-selected-specificity-bug.md](bds-date-picker-task31-presets-focus-selected-specificity-bug.md) — Task 31 presets sidebar styling: real bug — focus-visible+selected preset loses its primary background to a CSS-specificity collision with the base hover/focus/active rule (no `&--selected:focus-visible` override exists). Reported, not fixed. Also documents the `bds-transition-surface` 0.3s-transition read-timing gotcha and that programmatic `.focus()` never sets `:focus-visible` in Chromium — use real `keyboard.press('Tab')`.
- [bds-date-picker-task34-react-vue-presets-parity.md](bds-date-picker-task34-react-vue-presets-parity.md) — Task 34: presets sidebar (Task 30's 4 scenarios) confirmed identical across React/Vue/raw web components, zero divergence, zero console errors. Documents that Scenario 3's min/max disabled-preset set is date-relative (recompute before comparing to an older memory's specific list) and an open-popover coordinate-overlap gotcha on multi-picker pages.
- [bds-date-picker-task35-banner-footer-summary.md](bds-date-picker-task35-banner-footer-summary.md) — Task 35 (TC-23–TC-26): info banner positioning + footer range-summary all pass. Footer summary is raw elapsed time not inclusive day-count (Sept1→Sept18 = "17 days", not "18"). Playwright-cli technique for bds-select time dropdowns (multiple hidden listboxes, role=option only on selected item).
- [bds-date-picker-task39-banner-summary-wrapper-parity.md](bds-date-picker-task39-banner-summary-wrapper-parity.md) — Task 39: Phase 7 banner + footer range-summary confirmed byte-identical across raw web components / React / Vue, plus WebKit — zero wrapper divergence. Settles two Task-35 ambiguities: banner close also closes the whole popover (universal, reproduces in raw), and the banner box is 16px wider than the day grid by design (`padding-xs` vs `padding-l`). Also: `basic`+range summary is an *inclusive* day count (`resolveDurationRangeEndIso` adds +1 day when `!withTime`) — Sep 1→Sep 5 = "5 days".
- [bds-date-picker Task 45 min/max anchor out-of-range](bds-date-picker-task45-minmax-anchor-outofrange.md) — Task 45: React/Vue/raw/WebKit byte-identical across all 5 keyboard/live-region/Escape scenarios (no wrapper divergence). But with min/max narrowing and today outside the range, the grid re-anchors its roving cell on today (a disabled cell) on open, and keyboard focus lands there with arrow keys a total no-op — pre-existing, reproduces on raw. Also records the pre-existing 3333 stencil server → auto-fallback to 3334, the minified-served-HTML `grep -c` false-staleness trap, and that `run-code` has no `setTimeout`.
- [Date-picker ADR-0016 reopen/live grid mismatch — resolved](date-picker-reopen-shift-not-restored.md) — fixed by making the grid highlight the resolved span live; still always check grid DOM/screenshot on reopen for any real-anchor+computed-shift feature.
- [index.html unscoped gridcell query cross-contamination](index-html-unscoped-gridcell-query-cross-contamination.md) — scope DOM eval queries to the specific date-picker id; the playground accumulates dozens of mounted scenarios.
- [playwright-cli ref-click focus side effect](playwright-ref-click-focus-side-effect.md) — `click <ref>` pre-focuses the target; on a self-removing control (close button) it falsely closes popovers. Use coordinate mousemove/mousedown/mouseup for those.
- [bds-date-picker-task48c-nested-header-verified.md](bds-date-picker-task48c-nested-header-verified.md) — Task 48c: outer day-grid header now dims+`aria-hidden`/`inert`s alongside the day grid while a picker is open, and the picker gets its own separate nested header inside the card — all 4 checklist items pass, zero regressions, dual-instance independence holds.
- [bds-date-picker-task48i-quickpicker-pageupdown-verified.md](bds-date-picker-task48i-quickpicker-pageupdown-verified.md) — Task 48i: quick-picker PageUp/PageDown paging verified live (all 5 items PASS, Chromium + WebKit). Month view = ±1 year, year view = ±10 window, day view unchanged, min/max no-ops match disabled header buttons, dual-grid independent. Captures the stale-lazy-chunk misdiagnosis (`p-<hash>.entry.js` served OLD `PageDown` routing while the friendly entry was fresh — a fresh server process on a new port fixed it), the year-window focus-loss gotcha (cells keyed by year unmount → refocus between presses), and the year-picker header-button click-via-boundingBox technique.
- [bds-date-picker-task48e-quickpicker-keyboard-verified.md](bds-date-picker-task48e-quickpicker-keyboard-verified.md) — Task 48e: quick-picker keyboard nav + ARIA verified live (label Enter/Space fix confirmed with click-count=1, arrow/Home/End + wrap, month/year drill-down, Escape/backdrop dismissal, live-region single announcements, dual-grid independence) — all 7 items PASS, 0 console errors. Also captures reusable harness gotchas: forward-Tab from inside the popover can close it (use Shift+Tab to reach the label), two `.label-button`s coexist, MutationObserver-on-grid filtering, and click-count instrumentation for double-activation checks.
- [bds-date-picker-task52-quickpicker-wrapper-parity.md](bds-date-picker-task52-quickpicker-wrapper-parity.md) — Task 52: React == Vue deep-equality on the Phase 9 quick-picker (all checks match) EXCEPT the Vue testapp's `onTask52S2Change` `typeof e.detail !== 'string'` guard silently ignores Clear's empty-string payload (React sets it) — a testapp-handler divergence, not a component one. Plus reusable playwright-cli gotchas: `>> nth=0` chained selectors silently match nothing, `:text-is()` doesn't match a `bds-button` inner `<button>` (use `bds-button:nth-of-type(n) button`), popover footer buttons sit below the viewport (resize to 1280x1600 / boundingBox+mouse.click), `el.children` is unreliable on the light-DOM popover host.


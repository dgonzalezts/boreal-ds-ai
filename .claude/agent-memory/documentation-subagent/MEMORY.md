# Documentation Subagent — Memory Index

## Topic Files

- [verification_storybook_dev_docs_port_conflict.md](verification_storybook_dev_docs_port_conflict.md) — `pnpm dev:docs` hangs on an interactive port-6006-conflict prompt if a prior instance is still running; check `lsof -i :6006` first and reuse it, verify via `iframe.html?id=...` and the `#storybook-preview-iframe`'s `contentDocument` through Playwright MCP.
- [bds-table-row-detail-timing-bugs.md](bds-table-row-detail-timing-bugs.md) — `bds-table`'s row-detail toggle can fail to render on first paint (slot-relocation race) and `bdsExpand` fires before its detail DOM exists (async render-commit gap); both are real implementation bugs, not docs issues — story workarounds documented, real fix belongs to frontend-subagent.
- [argtypes-name-collision-across-subcomponents.md](argtypes-name-collision-across-subcomponents.md) — `<ArgTypes include={[...]}>` filters by resolved `name` across the entire shared `meta.argTypes` object, not scoped per sub-component; two sub-components with a same-named prop (`bds-table-column`/`bds-table-column-group`'s `label`/`info`) need a CSF3 per-story `argTypes` override, not a `name:` override, to disambiguate. Moved here from the incorrectly-placed `packages/boreal-web-components/.claude/agent-memory/documentation-subagent/` on 2026-07-31.
- [playwright-native-dnd-verification.md](playwright-native-dnd-verification.md) — `browser_drag` doesn't trigger native HTML5 DnD on `bds-table`'s reorder handles; manually dispatch `mousedown`→`dragstart`→`dragover`→`drop`→`dragend` via `browser_evaluate`, read the result in a separate call (Stencil re-renders in a microtask). Moved here from the incorrectly-placed `packages/boreal-web-components/.claude/agent-memory/documentation-subagent/` on 2026-07-31; also corrected a since-fixed `moveColumn` gotcha the original version described as a still-open workaround.
- [storybook-docs-page-story-id-mismatch.md](storybook-docs-page-story-id-mismatch.md) — `bds-table`'s Storybook doc/story ids are `data-visualization-table--*` (from `meta.title: 'Data Visualization/Table'`), not `data-visualization-bds-table--*` guessed from the tag name; project-wide, every docs page id ends `--overview`, never Storybook's default `--docs` — confirm via `curl localhost:6006/index.json`. Moved here from the incorrectly-placed `packages/boreal-web-components/.claude/agent-memory/documentation-subagent/` on 2026-08-01.
- [meta-args-defaults-defeat-unset-prop-demos.md](meta-args-defaults-defeat-unset-prop-demos.md) — a shared render function forwarding `args.X` unconditionally makes every story inherit `meta.args.X`'s non-empty default, silently defeating any story meant to demonstrate `X`'s true "left unset" behavior — override that prop explicitly to `''`/`undefined` in the story's own `args`, and verify via the rendered DOM property, not the Source panel.
- [bds-table-column-visibility-technique.md](bds-table-column-visibility-technique.md) — `bds-table-column` only reacts to `childList` DOM mutations (not attribute/style changes); `display:none` on it has zero visual effect — remove/reinsert the element to hide/show a column.
- [bds-dropdown-programmatic-open.md](bds-dropdown-programmatic-open.md) — synthetic `.click()` on a `bds-button` trigger never opens a `bds-dropdown` (`event.detail===0` guard in `bds-button`'s click handler) — call the child `bds-popover`'s `openPopover()` method directly instead, queried lazily.
- [nested-template-literal-double-backslash-escaping.md](nested-template-literal-double-backslash-escaping.md) — a `render:`-function's inline `<script>` text is a second JS source nested inside the outer Lit `html\`\`` template literal; single `\n`/`\t` there become real control chars at TS-compile time and can silently break (e.g. split a regex literal across lines) — always double-escape (`\\n`) inside inline `<script>` blocks, matching what `docs.source.code` already needs.

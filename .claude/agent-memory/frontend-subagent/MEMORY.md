# Frontend Subagent — Per-Scope Memory Index

- [SCSS placeholder refactor verification technique (EOA-17662 T36a)](scss-placeholder-refactor-verification-technique.md) — raw CSS diff is misleading for @extend dedups; compare per-selector effective-declarations maps instead; www/build hashed chunk grep as live-freshness proxy.
- [Banner state field is StatusVariant, not ComponentState (EOA-17662 T35)](bds-date-picker-banner-statusvariant-not-componentstate.md) — plan said reuse ComponentState but that can't express bds-banner's actual variant values; verify a plan's "reuse type X for prop Y" claim against Y's real source.
- [Lint/typecheck invocation](lint_typecheck_invocation.md) — `with-node.sh` cd's to repo root; use `pnpm --filter @telesign/boreal-web-components exec eslint|tsc` to scope checks, avoid turbo fan-out.
- [No strictNullChecks — lint:fix strips justified `!` (EOA-17662 T32a)](no-strictnullchecks-non-null-assertion-autofixed-away.md) — no strictNullChecks in tsconfig, so `x!` is a no-op and gets autofixed away; verify via clean `tsc --noEmit`, not assertion survival.
- [Presets time normalization (EOA-17662 T29)](bds-date-picker-presets-module-time-normalization.md) — date-fns endOfMonth carries 23:59:59.999; normalize to local midnight before 00:00/23:59 bounds; closed-string-set types belong in types/enum.ts even off-@Prop().
- [Value-contract widening hides call sites (EOA-17662 T23)](stencil-value-contract-widening-hidden-call-sites.md) — widening a value's string shape breaks readers a plan's Files list omits; grep every reader, don't trust the plan as exhaustive.
- [min/max nav-guard (EOA-17138 T11)](bds-date-picker-min-max-nav-guard.md) — rangeUnderflow/rangeOverflow keys, nav-guard lives in bds-date-picker not bds-calendar-grid, unregistered-child getAttribute() test gotcha.
- [Scoped CSS can't cross component boundary (EOA-17138 T19o)](stencil-scoped-css-cross-component-boundary.md) — Component A's .scss can't reach B's markup; inject a global unscoped `<style>` from JS; a `data-` attr survives re-render, inline `style` doesn't.
- [Dev server stale-chunk symptom catalog (EOA-17138 T19o)](stencil-dev-server-stale-chunk-symptom-catalog.md) — `customElements.get(tag).toString()` is the definitive live-source check; kill + rm -rf www/build + restart, not just reload.
- [Dual-calendar derived-state nav lock (EOA-17138 T19m-3)](stencil-dual-calendar-derived-state-nav-lock.md) — secondDisplayYear/Month became a getter derived from displayYear/Month, structurally preventing drift; handleMonthNavigate shifts one shared anchor pair.
- [tsc -p relative path picks wrong tsconfig](tsc-relative-path-picks-workspace-root-tsconfig.md) — pass an absolute path to `-p`, or it silently loads the workspace-root config and floods bogus errors.
- [Multi-instance nav event source ID (EOA-17138 T18, superseded by T19m-3)](stencil-multi-instance-nav-event-source-identification.md) — ref + event.target routes bdsMonthNavigate from sibling instances; still valid for genuinely independent siblings.
- [SCSS BEM double-element nesting bug (EOA-17138 T19i)](stencil-bem-double-element-nesting-bug.md) — `&__child` inside `&__parent{}` compounds into `__parent__child`; use the literal class instead of `&`.
- [Figma DatePickerNumber `actual` = isToday (EOA-17138 T19)](figma-datepicker-number-actual-axis-is-istoday.md) — not spec-vs-actual; no directional cap shape for range Start/End/Full.
- [date-engine TZDate conversion (EOA-16692 T1)](stencil-date-engine-tzdate-timezone-conversion.md) — TZDate ctor/accessor semantics for combineDateTimeToUTC/extractDateTimeFromUTC; DST offset-comparison test technique.
- [bds-calendar-grid render pattern (EOA-16692 T9)](stencil-calendar-grid-render-pattern.md) — BEM classes, tabIndex=-1 on all cells, aria-current/aria-selected, aria-label via formatDisplayDate 'PPPP', click guard.
- [bds-table column resize (EOA-16000 T6)](stencil-table-column-resize-pattern.md) — colgroup `<col>` is the real resize target, not `<th>`; imperative DOM write before throttled state commit.
- [table-layout:fixed width:auto GRIDMIN floor bug](stencil-table-layout-fixed-width-auto-gridmin-floor.md) — width:100% inflates on all-explicit columns; width:auto adds a real-browser-only min-content floor invisible to jsdom; fix with explicit calc()-summed table width.
- [React/Vue example apps serve stale wc code](example-apps-file-protocol-dep-staleness.md) — pnpm file: link + Vite cache can miss a fresh stencil rebuild; don't trust repros without an unambiguous freshness marker.
- [bds-table detail-row collapse inner-wrapper fix](stencil-table-detail-row-collapse-inner-wrapper-fix.md) — padding-floor grid-collapse fix via `.bds-table__detail-content-inner`; plus `&__tr-detail td { height: auto }`.
- [No shadow: false in decorator](feedback_no_shadow_false.md) — omit `shadow` from `@Component` entirely; Stencil defaults to light DOM.
- [No @default tags in JSDoc](feedback_no_jsdoc_default.md) — TS initializers are the authoritative default; `@default` tags duplicate and drift.
- [Render helper methods](feedback_render_helpers.md) — extract `renderRegion()` private methods when `render()` has multiple structural sections.
- [Utils in a dedicated folder](feedback_utils_folder.md) — component-local utilities go in a `utils/` subfolder, not inline in the `.tsx`.
- [slotchange: two patterns](feedback_slotchange_listener.md) — `<slot onSlotchange>` for slotted children; imperative `addEventListener` for unslotted direct DOM children.
- [No @summary in JSDoc](feedback_no_jsdoc_summary_tag.md) — CEM compiler discards @summary; leading prose before any @ tag is the sole "description" source.
- [Re-audit JSDoc on every API change](feedback_jsdoc_sync_on_change.md) — `@slot` tags drift silently; verify class JSDoc against render() before finishing any edit (bds-table incident).
- [Prop-or-slot pattern](feedback_prop_or_slot_pattern.md) — use `{prop.length > 0 ? <el> : <slot />}`, not slot fallback children (avoids DOM noise).
- [PREFIX constant](feedback_prefix_constant.md) — declare `const PREFIX = 'bds-tag' as const` at top; use in all class template literals.
- [classMap getter](feedback_classmap_getter.md) — `private get classMap(): StyleModifiers` for conditional classes; never inline ternaries in JSX class bindings.
- [SCSS BEM nesting](feedback_scss_bem_nesting.md) — nest all `&__element` rules inside the block selector; never flat `.bds-component__element`.
- [Component code organization](component-code-organization-bds-table.md) — `@Element()` before `@State()`; lifecycle stays in execution order; section comments omitted by preference.
- [Collapsed-trigger focus/scroll bug](feedback_scroll_into_view_collapsed_focusable.md) — manage tabindex on every internal focusable or reverse Tab bypasses expand handler and corrupts scrollLeft.
- [bds-search-bar scroll-clip fix (EOA-15204)](stencil-search-bar-scroll-clip-bug.md) — root cause + fix location; `--testPathPattern` doesn't scope correctly here.
- [Third-party prop string literals](feedback_thirdparty_prop_string_literals.md) — nested bds-* props (e.g. `mode="search"`) use string literals, not the target's enum import.
- [Imperative content into non-shadow slot](feedback_imperative_content_into_nonshadow_slot.md) — never `.textContent` a `shadow:false` host; ref a plain child node inside its slot instead.
- [Fresh worktree missing style-guidelines dist](stencil-worktree-missing-dist-dependency.md) — sass "Can't find stylesheet" in a new worktree means build `@telesign/boreal-style-guidelines` first, not a code bug.
- [Slot relocation timing](feedback_slot_relocation_timing.md) — non-shadow slot polyfill relocates children before `componentDidLoad`; inspect in `componentWillLoad` instead.
- [mock-doc has no :scope selector](stencil-mockdoc-no-scope-selector.md) — `newSpecPage` throws on `:scope`; use `Array.from(el.children).find(...)`.
- [Node relocation breaks on re-render](stencil-node-relocation-breaks-on-rerender.md) — cache a relocated light-DOM node by key; re-querying its old parent after move wrongly unmounts it (bds-table T3).
- [JSX Fragment shorthand throws at test runtime](feedback_jsx_fragment_shorthand.md) — never `<>...</>`; import `<Fragment>` from `@stencil/core` explicitly.
- [Radius token lookup](feedback_radius_token_lookup.md) — check `dist/scss/variables/_primitives.scss` for resolved px; shared skeleton classes win over a per-node Figma re-read.
- [Hide real slotted component imperatively](stencil_imperative_hide_slotted_component.md) — no wrapper div; query the slotted element directly, toggle `style.display` in `componentDidRender`.
- [vdom key forces element recreation](stencil_vdom_key_forces_element_recreation.md) — give a distinct `key` per branch when alternating imperative vs declarative content, or stale DOM survives the patch.
- [Guard expensive read, not cheap write](feedback_guard_expensive_read_not_cheap_write.md) — split a guarded DOM-sync pass into cached-read + unconditional-write, or virtualized vdom swaps in unstyled elements.
- [Dev server stale hashed chunk](stencil-dev-server-hashed-chunk-stale-cache.md) — `pnpm dev:components` can serve a stale chunk after rebuild; verify via network-fetched URL, restart if stale.
- [Virtual table needs maxHeight to bound DOM](stencil-virtual-table-needs-maxheight-to-bound-dom.md) — without a bounded wrapper, the virtualizer window equals full content height and renders nearly all rows.
- [Pure-reorder verification technique](feedback_pure_reorder_verification_technique.md) — script-driven line-range extraction + sorted-diff + before/after test run, for large no-logic-change reorders.
- [CLAUDE.md overrides jsdoc-template.md on internal JSDoc](feedback_claude_md_no_internal_jsdoc.md) — "public API only" means strip ALL `/** */` from internals, but only when the user explicitly directs it.
- [Internal types belong in types/ folder](feedback_internal_types_in_types_folder.md) — even non-exported implementation-only types go in types/IComponent.ts, never inline in the .tsx.
- [forceUpdate race fix](stencil-forceupdate-race-fix.md) — `forceUpdate(this)` from componentDidLoad/MutationObserver fixes a first-paint race invisible to newSpecPage.
- [Defer event to componentDidRender](feedback_defer_event_to_componentDidRender.md) — queue an @Event's detail and flush at end of componentDidRender so listeners can query DOM a ref just created.
- [Descendant-selector false positive on "duplicate row"](feedback_descendant_selector_false_positive_bug_report.md) — compare direct-children count vs broad descendant-selector count before touching render logic.
- [Scoped stencil dev server invocation](stencil-dev-server-direct-invocation.md) — `with-node.sh bash -c 'cd packages/boreal-web-components && exec node_modules/.bin/stencil build --dev --watch --serve --port N'`.
- [mock-doc template.content vs childNodes](stencil-mockdoc-template-content-vs-childnodes.md) — `appendChild` on a mock `<template>` writes `childNodes`, only `innerHTML` populates `.content`.
- [MutationObserver retry for late template.content](stencil-mutationobserver-retry-for-late-template-content.md) — React populating `<template>.content` after first render needs a one-shot MutationObserver + forceUpdate.
- [componentWillLoad sees 0 children in real browser](stencil-column-discovery-componentdidload-refresh-fix.md) — `this.el.children` is reliably empty pre-load for a lazy non-shadow component; re-run discovery in `componentDidLoad` too.
- [table-layout:fixed + colgroup fix](stencil-table-layout-fixed-colgroup.md) — fixed layout only reads widths from the first `<tr>`; a multi-row `<thead>` needs a `<colgroup>`/`<col>` instead.
- [CDN ESM process.env shim](cdn-esm-process-env-shim.md) — CDN ESM packages referencing `process.env.NODE_ENV` throw in bundler-free pages; shim `window.process` via a classic `<script>` first.
- [bds-table th leading-icon layout](bds-table-th-leading-icon-layout.md) — `<th>` isn't flex by default; scope `th:has(> .icon-class) { display: flex }`, never a blanket `th { display: flex }`.
- [Keyed `<th>` move blurs focus](stencil-keyed-th-move-blurs-focus.md) — `insertBefore`-moving a focused connected node blurs it; keyed reorders need an explicit post-render `.focus()` restore.
- [bds-table rowClickSelects pattern (EOA-16000 T12)](stencil-table-rowclickselects-pattern.md) — reuses handleRowSelect's rowSelectable guard; module-level excluded-selector const; prop/method ordering conventions.
- [Dual-direction pin offset guard state must not be shared](stencil-dual-direction-pin-offset-guard-state.md) — mirrored recompute methods need fully separate cache/guard fields or the second call in a tick sees "unchanged".
- [date-fns compareAsc returns number, not literal union](stencil-date-fns-compareasc-number-not-literal-union.md) — clamp explicitly with if/else, don't `as`-cast, for a -1|0|1 comparator return type.
- [stencil test --testPathPattern mangles into per-char regex](stencil-testpathpattern-vs-passthrough-arg.md) — use a bare path after `--` instead; override `--coverageThreshold='{}'` + `--collectCoverageFrom` for scoped coverage.
- [Component types/ dirs use index.ts barrels in practice](component-types-dir-barrel-convention.md) — contradicts development-standards.md §3.3's "no barrel files"; most multi-file `types/` dirs barrel anyway.
- [date-fns format()'s locale option is optional](date-fns-format-locale-optional.md) — `{ locale: undefined }` safely falls back to default locale, no guard needed.
- [Nested component same-name event collision](stencil-nested-component-same-name-event-collision.md) — a child's bare @Event() sharing the parent's event name bubbles spurious duplicates; caught only live.
- [Mixin lifecycle silently shadowed without override+super](stencil-mixin-lifecycle-override-silent-shadow.md) — adding componentWillLoad etc. to a Mixin class without `override`+`super.x()` drops the mixin's own init.
- [pnpm build turbo cache-hit false positive](stencil-turbo-build-cache-hit-false-positive.md) — `pnpm build` from inside a package dir can replay a stale cached Turbo log; use `pnpm --filter ... build` for a real check.
- [Conditional slot-target orphans parent content](stencil-conditional-slot-target-orphans-parent-content.md) — toggling a child's prop false doesn't hide the parent's own unconditional `slot="..."` JSX; gate it with the same condition.
- [Presets sidebar Figma token mapping (EOA-17662 T31)](bds-date-picker-presets-figma-token-mapping.md) — full State×Selected resolution for `_DatePickerRange`; Focus non-additive with Selected; Active uses inset box-shadow token, not the focus-ring mixin; zero gap between preset buttons.
- [Banner/footer Figma audit (EOA-17662 T36)](bds-date-picker-banner-footer-figma-audit.md) — unbound 10px literal padding resolved to nearest token; footer label/value gap (4px) differs from header's (2px); popover-footer space-between vs Figma's flex-end left as documented, out-of-scope discrepancy; hidden Figma frames are still pullable via get_design_context.
